.boxhead
  %h3 Categories
.box
  %ul.categories
    - Category.all.each do |category|
      %li
        = link_to category.name, root_url(:category => category.name)
        .clear
      %li.spacer
        .clear
